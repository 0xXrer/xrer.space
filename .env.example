# GitHub Personal Access Token (optional)
# Without this token, the GitHub API is limited to 60 requests per hour
# With a token, the limit increases to 5000 requests per hour
#
# To create a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "Portfolio Website"
# 4. Select scopes: public_repo (or just leave all unchecked for public data only)
# 5. Click "Generate token"
# 6. Copy the token and paste it below
#
# Then copy this file to .env and add your token
GITHUB_TOKEN=your_github_token_here
DISCORD_BOT_TOKEN=your_bot_token_here
